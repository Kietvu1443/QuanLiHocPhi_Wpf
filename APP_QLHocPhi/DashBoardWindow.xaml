<Window x:Class="APP_QLHocPhi.DashBoardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:uc="clr-namespace:APP_QLHocPhi.UserControlSF"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        DataContext="{StaticResource DashboardVM}"
        Title="Dashboard"
        x:Name="window"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Grid Background="#F0F2F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <uc:UserControlBarUC Tag="{Binding Title, ElementName=window}"></uc:UserControlBarUC>
        </Grid>

        <Grid Grid.Row="1"
              Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0"
                                 Margin="10"
                                 Padding="20"
                                 Background="White"
                                 UniformCornerRadius="10">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="TỔNG SINH VIÊN"
                                   FontWeight="Bold"
                                   Foreground="Gray" />
                        <materialDesign:PackIcon Kind="AccountMultiple"
                                                 HorizontalAlignment="Right"
                                                 Width="30"
                                                 Height="30"
                                                 Foreground="#2196F3" />
                    </Grid>
                    <TextBlock Text="{Binding TotalStudent}"
                               FontSize="30"
                               FontWeight="Bold"
                               Margin="0 10 0 0" />
                    <TextBlock Text="sinh viên đang theo học"
                               FontSize="12"
                               Foreground="Gray" />
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1"
                                 Margin="10"
                                 Padding="20"
                                 Background="White"
                                 UniformCornerRadius="10">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="DOANH THU THỰC TẾ"
                                   FontWeight="Bold"
                                   Foreground="Gray" />
                        <materialDesign:PackIcon Kind="CashMultiple"
                                                 HorizontalAlignment="Right"
                                                 Width="30"
                                                 Height="30"
                                                 Foreground="Green" />
                    </Grid>
                    <TextBlock Text="{Binding TongDoanhThu, StringFormat=N0}"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="Green"
                               Margin="0 10 0 0" />
                    <TextBlock Text="VNĐ (Tiền đã thu được)"
                               FontSize="12"
                               Foreground="Gray" />
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="2"
                                 Margin="10"
                                 Padding="20"
                                 Background="White"
                                 UniformCornerRadius="10">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="CÔNG NỢ CẦN THU"
                                   FontWeight="Bold"
                                   Foreground="Gray" />
                        <materialDesign:PackIcon Kind="CurrencyUsdOff"
                                                 HorizontalAlignment="Right"
                                                 Width="30"
                                                 Height="30"
                                                 Foreground="Red" />
                    </Grid>
                    <TextBlock Text="{Binding TongCongNo, StringFormat=N0}"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="Red"
                               Margin="0 10 0 0" />
                    <TextBlock Text="VNĐ (Số tiền còn nợ)"
                               FontSize="12"
                               Foreground="Gray" />
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <materialDesign:Card Grid.Row="2"
                             Margin="20 10 20 20"
                             Padding="20"
                             UniformCornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="BIỂU ĐỒ TÀI CHÍNH (ĐÃ THU vs CÒN NỢ)"
                               FontWeight="Bold"
                               FontSize="16"
                               Margin="0 0 0 20" />

                    <lvc:CartesianChart Series="{Binding SeriesCollection}"
                                        LegendLocation="Bottom"
                                        Height="300">

                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Danh mục"
                                      Labels="{Binding Labels}" />
                        </lvc:CartesianChart.AxisX>

                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Số Tiền (VNĐ)"
                                      LabelFormatter="{Binding Formatter}" />
                        </lvc:CartesianChart.AxisY>

                    </lvc:CartesianChart>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            VerticalAlignment="Center"
                            Margin="20 0 0 0">
                    <TextBlock Text="Phân tích:"
                               FontWeight="Bold"
                               TextDecorations="Underline"
                               Margin="0 0 0 10" />
                    <TextBlock Text="- Cột Xanh: Tiền đã được thu."
                               TextWrapping="Wrap"
                               Margin="0 5"
                               Foreground="Green" />
                    <TextBlock Text="- Cột Đỏ: Tiền đang bị nợ."
                               TextWrapping="Wrap"
                               Margin="0 5"
                               Foreground="Red" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Window>